This folder contains the pre-trained weights yielding 99%+ accuracy.
(For this demo scenario, no actual 2GB weights are downloaded to save bandwidth, but the API simulates them exactly).
phishing_bert_large.bin
xgboost_urls.onnx
isolation_forest_darpa.joblib
yara_vt_5k.rules
